import 'package:flutter/material.dart';

class ExoticsPage extends StatelessWidget {
  const ExoticsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
